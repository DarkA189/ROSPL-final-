from operator import mod
from django.db import models

# Create your models here.
'''class document_scanner_storage(models.Model):
    key = models.CharField(maxlength=255,default=0)
    image = models.ImageField(upload_to="docscan/", default="")'''